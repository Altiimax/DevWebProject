import React from "react";
import { Modal, Button, Nav, Navbar } from "react-bootstrap";

const Testnav = (props) => {
  return (
    <Button
        variant="primary"
    >
    COUCOU
    </Button>
  );
};
export default Testnav;